import React from 'react';


const Login = () => {
    return (
        <>
            <button onClick={() => window.location.href = '/login'}>ログイン</button>
        </>
    );
};

export default Login;